# Local automated PostgreSQL setup.
# Run from the repo root:
#   npm.cmd run setup:local
DATABASE_URL="postgresql://motionise@localhost:5433/motionise?schema=public"

PORT=3001
NODE_ENV=development

# File storage, relative to backend/
UPLOADS_DIR=./uploads
