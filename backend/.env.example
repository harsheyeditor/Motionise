# ── Neon PostgreSQL connection string
# Get from: https://console.neon.tech → your project → Connection string (select "Node.js")
DATABASE_URL="postgresql://user:password@ep-xxxx.us-east-2.aws.neon.tech/motionise?sslmode=require"

# ── Server
PORT=3001
NODE_ENV=development

# ── File storage (relative to backend/)
UPLOADS_DIR=./uploads
